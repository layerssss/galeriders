version: '3'
services:
  ui:
    image: registry.micy.in/layerssss/galeriders:ui
    restart: always
    ports:
      - "80"
  api:
    image: registry.micy.in/layerssss/galeriders:api
    restart: always
    command: "bin/rails server --port 80"
    links:
      - db
    ports:
      - "80"
    volumes:
      - "log:/rails/log"
      - "data:/rails/data"
  migrator:
    image: registry.micy.in/layerssss/galeriders:api
    restart: on-failure
    command: "bin/rails db:migrate"
    links:
      - db
    volumes:
      - "log:/rails/log"
      - "data:/rails/data"
  db:
    image: postgres:9.5
    restart: always
    environment:
      POSTGRES_PASSWORD: mysecretpassword
      POSTGRES_USER: pguser
      POSTGRES_DB: grapi
    volumes:
      - 'pg_data:/var/lib/postgresql/data'
    ports:
      - '5432'
volumes:
  pg_data: {}
  log: {}
  data: {}
